# 🚨 긴급: Railway 환경 변수 설정 필요

## 문제 상황
인스타그램 계정 최적화 기능에서 500 에러가 발생하고 있습니다.

## 원인
Railway 서버에 GrowthCore API 키가 설정되지 않았습니다.

## 해결 방법 (5분 소요)

### 1단계: Railway 대시보드 접속

1. https://railway.app 접속
2. 로그인
3. 프로젝트 선택 (cursor-production)

### 2단계: Backend 서비스 선택

1. **"cursor-server"** 또는 **"Backend"** 서비스 클릭
2. 상단 탭에서 **"Variables"** 선택

### 3단계: 환경 변수 추가

**"New Variable"** 버튼을 클릭하고 다음을 입력:

```
Variable Name: ACCOUNT_OPTIMIZATION_API_KEY
Value: [여기에 GrowthCore API 키를 입력하세요]
```

> 💡 API 키는 GrowthCore 담당자로부터 받은 문자열입니다.
> 예: `gc_live_abc123def456...` (실제 키로 교체)

### 4단계: 저장 및 재시작

1. **"Add"** 버튼 클릭
2. Railway가 자동으로 서비스를 재시작합니다 (30초~1분 소요)

### 5단계: 배포 확인

1. Railway에서 **"Deployments"** 탭 클릭
2. 가장 최근 배포가 **"SUCCESS"** 상태가 될 때까지 대기
3. **"View Logs"** 클릭하여 로그 확인

정상 로그 예시:
```
✅ OK: globalSearch.js uses phone only for retargeting_customers
Server listening on port 3000
```

에러가 있다면 로그를 복사하여 개발팀에 전달해주세요.

---

## 테스트

### 웹사이트에서 테스트

1. https://www.hotseller-crm.com 접속
2. 로그인
3. 우측 상단 톱니바퀴(⚙️) 클릭
4. **"계정 최적화 조회"** 클릭
5. 인스타그램 ID 입력: `gono_92`
6. **"分석하기"** 버튼 클릭

**정상 작동**: 프로필 사진, 팔로워 수, 분석 결과가 표시됨  
**여전히 에러**: 아래 "트러블슈팅" 참고

---

## 트러블슈팅

### 문제 1: "外部連携キーが設定されていません" 메시지
✅ **해결**: API 키를 Railway Variables에 추가 (위 3단계 참고)

### 문제 2: 500 에러가 계속 발생
🔍 **확인**:
1. Railway > Backend > **"Variables"** 탭
2. `ACCOUNT_OPTIMIZATION_API_KEY`가 존재하는지 확인
3. API 키 값이 올바른지 확인 (오타, 공백 없는지)

📋 **로그 확인**:
1. Railway > Backend > **"Deployments"** > 최신 배포 클릭
2. **"View Logs"** 클릭
3. 다음과 같은 로그를 찾으세요:
   ```
   [AccountOptimization] Request received: { id: '...' }
   [AccountOptimization] Using endpoint: ...
   [AccountOptimization] Error details: ...
   ```
4. 에러 메시지를 복사하여 개발팀에 전달

### 문제 3: 배포가 진행되지 않음
🔄 **수동 재배포**:
1. Railway > Backend 서비스
2. 우측 상단 **"···"** 메뉴 클릭
3. **"Redeploy"** 선택

---

## 추가 정보

### 코드 변경 사항
- ✅ 서버 코드에 상세한 로깅 추가
- ✅ 에러 처리 강화
- ✅ GitHub에 푸시 완료 (`10d312dbd`)
- ✅ Railway 자동 배포 진행 중

### 설정해야 할 환경 변수 (체크리스트)
- [x] `DATABASE_URL` (자동 설정됨)
- [x] `JWT_SECRET`
- [x] `CORS_ORIGIN`
- [ ] **`ACCOUNT_OPTIMIZATION_API_KEY`** ← **이것을 지금 추가해야 합니다!**

---

## 도움이 필요하신가요?

다음 정보를 함께 보내주세요:
1. Railway 로그 전체 (Deployments > View Logs)
2. 브라우저 개발자 도구 > Console 탭의 에러 메시지
3. Network 탭에서 실패한 요청의 Response

📧 개발팀 연락처: [연락처 입력]

---

**예상 소요 시간**: 5분  
**영향 범위**: 인스타그램 계정 최적화 기능만 해당  
**긴급도**: 높음 (기능 중단)

---

## 빠른 확인 명령어

터미널에서 API 테스트:
```bash
curl -X GET "https://cursor-production-1d92.up.railway.app/api/account-optimization?id=gono_92" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json"
```

Health Check:
```bash
curl https://cursor-production-1d92.up.railway.app/api/health
```

