[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.build]
cmds = [
  'npm ci',
  'rm -rf dist',
  'rm -rf node_modules/.cache',
  'npm run build',
  'echo "✅ Build completed"'
]

[start]
cmd = 'node dist/index.js'
