[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.build]
cmds = [
  'npm ci',
  'echo "✅ Using pre-built dist folder from Git"',
  'ls -la dist/routes/accounting.js',
  'grep -c "Best encoding" dist/routes/accounting.js && echo "✅ New code verified"'
]

[start]
cmd = 'echo "=== VERIFYING BUILD ===" && grep -c "Best encoding" dist/routes/accounting.js && echo "✅ New code verified" && node dist/index.js'
